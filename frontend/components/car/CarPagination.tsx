'use client'

// Re-export the shared Pagination component for backward compatibility.
// New code should import from '@/components/ui/Pagination' directly.
export { default } from '@/components/ui/Pagination'
